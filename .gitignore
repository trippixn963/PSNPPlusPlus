node_modules/
sidecar/.venv/
sidecar/state.db
sidecar/state.db-wal
sidecar/state.db-shm
sidecar/.env
__pycache__/
.DS_Store
dist/.psnppp-inner.js

# Deployment target, host-specific checks, and the operator's own runbook.
# This repo is public; nobody's server address, key path, or neighbouring
# services belong in it. sidecar/deploy/RUNBOOK.local.md is the real one, with
# real hostnames; sidecar/deploy/DEPLOY.md is the generic guide that ships.
scripts/deploy.env
scripts/*.local.sh
*.local.md
